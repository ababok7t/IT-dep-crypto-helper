run:
  deadline: 2m
  issues-exit-code: 1
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
linters-settings:
  errcheck:
    exclude:
      - Error return value of .* is not checked